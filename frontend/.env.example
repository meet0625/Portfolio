# Optional: endpoint the contact form POSTs { name, email, message } JSON to.
# If left unset, the contact form falls back to opening the visitor's email client.
# Example: a Formspree endpoint like https://formspree.io/f/xxxxxxx
VITE_FORM_ENDPOINT=
